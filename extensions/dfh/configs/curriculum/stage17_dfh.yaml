# @package _global_
## ---------------------------------------------------------------------------
#  Curriculum Stage 1.7 DFH — geometry bridge (S2 geometry + intermediate soil)
## ---------------------------------------------------------------------------

defaults:
  - /terrain: terrain_dfh_stage17_geom
  - /domain_rand: DR_dfh_soft
  - /rewards: loco/reward_hunter_dfh

curriculum:
  stage_id: "stage17_dfh"
  promotion:
    metric_mean_episode_length_s_min: 17.5
    metric_term_reward_per_s_min:    -0.035
    require_nonzero_feet_air_time:    True

env:
  config:
    env_spacing: 2.5
