[pytest]
markers =
    unit: pure unit tests, no IsaacSim, no GPU required
    integration: integration tests requiring IsaacSim/IsaacLab
    gpu: tests that require a CUDA device
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
