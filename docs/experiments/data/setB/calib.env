# Set B calibration (from fixed arm block-0 mu=0.80 mean ep_len)
# averaged over 3 fixed seed(s); ep_len_cap=1000; mastery0=0.3875
# crisp advances when mastery>=CRISP_T; fuzzy breaks centred on CRISP_T
CRISP_T=0.29
FUZZY_BREAKS=0.17,0.29,0.41
